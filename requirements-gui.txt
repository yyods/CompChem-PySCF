# Desktop client — install on the HOST, not in the container.
# python:3.10-slim ships no Qt platform libraries; PySide6 does have native
# Windows wheels, so the GUI runs where PySCF cannot, and vice versa.
PySide6==6.11.2
pyinstaller==6.22.2